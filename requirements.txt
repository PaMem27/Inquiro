# Paper ingestion
arxiv
requests==2.32.3
pypdf

# LangChain / LangGraph stack
langchain==1.3.4
langchain-core==1.4.0
langchain-text-splitters==1.1.2
langchain-chroma==1.1.0
langchain-groq
langchain-huggingface==1.2.2
langgraph==1.2.4

# Vector store, embeddings, and LLM
chromadb==1.5.9
# Embedding stack pinned to a verified-compatible set; newer combinations have
# broken batch tokenization (TextEncodeInput error) during ingestion.
sentence-transformers==5.5.1
transformers==5.10.1
tokenizers==0.22.2

# Config and UI
python-dotenv==0.21.0
streamlit==1.42.0
